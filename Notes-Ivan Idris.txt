复数不能转化为整数和浮点数 int(4.0+1.j) TypeError
但浮点数可以转化为复数 complex(1.0)

Numpy数组中的每个元素均为相同的数据类型，对单个数组元素在内存中占用的字节数 a.dtype.itemsize

在用array函数创建数组时，如果没有在参数中指定数据类型，将默认为浮点数类型。

如果在多维数组中执行翻转b[::-1],将在最前面的维度上翻转元素的顺序，即首维上翻转。


